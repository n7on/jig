[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "grim"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1",
]

[tool.setuptools]
packages = []
